name: smart_cart_ui
description: "Smart Cart Shopper App — Digital cart + BLE beacon broadcaster"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  beacon_broadcast: ^0.3.0
  http: ^1.4.0
  google_fonts: ^6.2.1
  permission_handler: ^11.0.0
  provider: ^6.1.0
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
